spring:
  datasource:
    url: jdbc:h2:file:./data/tododb2
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: update

cors:
  allowed-origins: http://localhost:5173